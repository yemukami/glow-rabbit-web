# 接続・同期まわりの課題タスク（次スレ用）

- 接続フィードバック経路の一本化: 成功/失敗/切断すべてでヘッダー更新＋トースト（設定でダイアログ任意）を確実に発火させる。
- 設置同期ステータス: dirty/clean状態をヘッダー表示に確実反映し、自動同期失敗時は失敗通知・再試行導線を出す。
- 同期バッジの信頼性: syncNeededバッジが折りたたみ/展開に関わらず最新状態を示すよう再描画タイミングを揃える。
- startPos挙動確認: 実機でstartPosがstartRunnerに反映されない場合のUI側補正（表示側でのcurrentDist初期化など）を検討。
