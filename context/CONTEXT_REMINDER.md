# 作業着手時の注意 (要コピペ)

1. `REMORSE_LOG.md` を確認し、再発防止策を遵守する（共有状態への直接再代入禁止、mutator使用、`node --check` 実行、該当UI操作の手動確認）。
2. `REQUEST_ACTIONS_TEMPLATE.md` をコピーして、要求内容・目的・方針・テスト計画・結果・備考を冒頭に記載する。
3. 変更が挙動に影響しそうな場合は、人間確認が必要なポイントを明示し、E2Eテスト手順（画面からの操作手順）を提示する。
4. バージョンはプッシュごとに上げる。
5. 作業/思考ログを `TODAY_LOG.md` に残す。
